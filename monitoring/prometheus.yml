global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "skillsphere-backend"
    metrics_path: /api/health
    static_configs:
      - targets: ["backend:8001"]
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
